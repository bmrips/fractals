## To Do

- Use the POSIX file interface
- Evaluate the performance of complex.h

## Done

- Merge the final '\n's to the arrays (see line 264f.)
- Use a logarithmic power calculation algorithm
- Use the binary PGM format
- Parametrise the iterations value cap in the convergence image
- Pass the interval to calculate as option
